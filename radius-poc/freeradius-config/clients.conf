# RADIUS clients (NAS devices allowed to talk to this server)
# Our Python PoC client runs on the host / same Docker network

client poc-nas {
    ipaddr      = 0.0.0.0/0        # accept from any IP (PoC only — lock this down in prod)
    secret      = testing123
    shortname   = poc-nas
    nas_type    = other
}
